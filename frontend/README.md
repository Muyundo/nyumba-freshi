# Nyumba Freshi — Frontend

Quick start (Windows PowerShell):

```powershell
cd frontend
npm install
npm run dev      # starts Vite dev server at http://localhost:5173/

# build for production
npm run build
```

Notes:
- This project uses Vite + React. Node.js (LTS) is required.
- After `npm install` a `package-lock.json` will be created.
