# JWT settings
# Use a strong random secret in production
JWT_SECRET=replace_with_a_strong_secret
JWT_EXPIRES_IN=1h

# Database: Using SQLite (no additional configuration needed)
# Database file will be created at: server/nyumba_freshi.db
